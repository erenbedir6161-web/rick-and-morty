import {Book1,PresentationChart,LocationTick,Setting2} from 'iconsax-react-native'

import {CHARACTERS, EPISODES, LOCATIONS, SETTINGS} from '../utils/routes'

const TabIcon = () => {
  return (
    <View>
      <Text>tabicon</Text>
    </View>
  )
}

export default TabIcon